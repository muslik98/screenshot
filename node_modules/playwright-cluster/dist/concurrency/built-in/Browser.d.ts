import * as playwright from 'playwright';
import ConcurrencyImplementation, { WorkerInstance } from '../ConcurrencyImplementation';
export default class Browser extends ConcurrencyImplementation {
    init(): Promise<void>;
    close(): Promise<void>;
    browser: playwright.Browser | null;
    workerInstance(perBrowserOptions: playwright.LaunchOptions | undefined, perPageOptions: playwright.BrowserContextOptions | undefined): Promise<WorkerInstance>;
}
